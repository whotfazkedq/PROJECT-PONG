hspeed = choose(-1, 1) * global.initial_ball_speed;
vspeed = choose(-1, 1) * global.initial_ball_speed;

vx = choose(-1, 1) * global.initial_ball_speed;
vy = choose(-1, 1) * global.initial_ball_speed;